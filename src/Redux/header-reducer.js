let initialState = {
    name: "Damir",
    subname: "Minibaiev"
};

const headerReducer = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state;
    }
}

export default headerReducer;